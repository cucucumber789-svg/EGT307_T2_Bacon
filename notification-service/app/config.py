"""
Application configuration for Notification Service.

Intention:
- Store SMTP email settings (host, port, credentials)
- Store SMS API credentials (if applicable)
- Load all config from environment variables
"""

# TODO: Define Config class with SMTP_HOST, SMTP_PORT, ALERT_THRESHOLD, etc.
