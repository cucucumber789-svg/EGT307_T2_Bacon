apiVersion: v1
kind: Service
metadata:
  name: ml-service
  labels:
    app: ml-service
spec:
  type: ClusterIP
  selector:
    app: ml-service
  ports:
    - port: 80
      targetPort: 5001
      protocol: TCP
  # TODO: Change to NodePort or LoadBalancer if external access needed
