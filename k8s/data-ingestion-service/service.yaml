apiVersion: v1
kind: Service
metadata:
  name: data-ingestion-service
  labels:
    app: data-ingestion-service
spec:
  type: ClusterIP
  selector:
    app: data-ingestion-service
  ports:
    - port: 80
      targetPort: 5003
      protocol: TCP
  # TODO: Change to NodePort or LoadBalancer if external access needed
