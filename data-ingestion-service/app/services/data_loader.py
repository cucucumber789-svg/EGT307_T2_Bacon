"""
Data loader service — handles parsing and forwarding sensor data.

Intention:
- Parse CSV files into structured records
- Parse JSON payloads into structured records
- Validate data before forwarding to Backend API
- Handle batch uploads efficiently
"""

# TODO: Define parse_csv(), parse_json(), forward_to_backend() functions
